/** babel.config.cjs是一个用于配置Babel编译工具的文件。
  * Babel是一个广泛使用的JavaScript编译器，它可以将新版本的
  * JavaScript代码转换为向后兼容的旧版本，以便在不同的浏览器和环境中运行。
 **/
module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset'
  ]
}
